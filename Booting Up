local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local CoreGui = game:GetService("CoreGui")

--==================================================
-- REMOVE OLD
--==================================================

pcall(function()
	local Old = CoreGui:FindFirstChild("MiniLibraryBoot")
	if Old then
		Old:Destroy()
	end
end)

--==================================================
-- BOOT GUI
--==================================================

local BootGui = Instance.new("ScreenGui")
BootGui.Name = "MiniLibraryBoot"
BootGui.ResetOnSpawn = false
BootGui.Parent = CoreGui

local Boot = Instance.new("Frame")
Boot.Size = UDim2.fromOffset(230,120)
Boot.Position = UDim2.new(0.5,-115,0.5,-60)
Boot.BackgroundColor3 = Color3.fromRGB(25,25,25)
Boot.BorderSizePixel = 0
Boot.Parent = BootGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0,10)
Corner.Parent = Boot

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,30)
Title.Position = UDim2.fromOffset(0,12)
Title.BackgroundTransparency = 1
Title.Text = "Mini Library"
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 22
Title.Parent = Boot

local Status = Instance.new("TextLabel")
Status.Size = UDim2.new(1,0,0,20)
Status.Position = UDim2.fromOffset(0,45)
Status.BackgroundTransparency = 1
Status.Text = "Booting up..."
Status.TextColor3 = Color3.fromRGB(190,190,190)
Status.Font = Enum.Font.SourceSans
Status.TextSize = 16
Status.Parent = Boot

local BarBack = Instance.new("Frame")
BarBack.Size = UDim2.fromOffset(190,7)
BarBack.Position = UDim2.new(0.5,-95,0,82)
BarBack.BackgroundColor3 = Color3.fromRGB(50,50,50)
BarBack.BorderSizePixel = 0
BarBack.Parent = Boot

local BarCorner = Instance.new("UICorner")
BarCorner.CornerRadius = UDim.new(1,0)
BarCorner.Parent = BarBack

local Bar = Instance.new("Frame")
Bar.Size = UDim2.new(0,0,1,0)
Bar.BackgroundColor3 = Color3.fromRGB(110,110,110)
Bar.BorderSizePixel = 0
Bar.Parent = BarBack

local BarFillCorner = Instance.new("UICorner")
BarFillCorner.CornerRadius = UDim.new(1,0)
BarFillCorner.Parent = Bar

--==================================================
-- BOOT
--==================================================

local Progress = TweenService:Create(
	Bar,
	TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
	{
		Size = UDim2.new(1,0,1,0)
	}
)

Progress:Play()

task.wait(0.5)
Status.Text = "Loading library..."

task.wait(0.6)
Status.Text = "Loading components..."

task.wait(0.6)
Status.Text = "Almost ready..."

Progress.Completed:Wait()

Status.Text = "Ready!"

task.wait(0.4)

--==================================================
-- LOAD WINDOW LIBRARY
--==================================================

local Library = loadstring(game:HttpGet(
	"https://raw.githubusercontent.com/DodoBallls/very-very-gud-UI-library-trust/refs/heads/main/Window"
))()

-- Remove boot screen
BootGui:Destroy()

--==================================================
-- CREATE YOUR WINDOW
--==================================================

local Window = Library:CreateWindow("My UI")

--==================================================
-- NOW YOU CAN ADD ELEMENTS
--==================================================

Window:CreateButton("HiExample", 1, function()
	print("HiExample clicked!")
end)

Window:CreateToggle("Example Toggle", 2, false, function(Value)
	print("Toggle:", Value)
end)

Window:CreateSlider("Example Slider", 3, 0, 100, 50, function(Value)
	print("Slider:", Value)
end)